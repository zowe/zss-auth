{
  "name": "zss-auth",
  "version": "1.0.0",
  "main": "lib/zssAuth.js",
  "scripts": {
    "build": "exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "zlux-server-framework": "file:../zlux-server-framework"
  }
}
